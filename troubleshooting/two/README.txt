Using Methods:

Steps through solving the example in the following sequence
- create print_multiples_numbers method and call it
- Fix the loop counter
- create the number_is_multiple(i) method
- Call `number_is_multiple(12)`
- Call `number_is_multiple(13)`
- simplify `number_is_multiple` to only i / 3
- simplify `number_is_multiple` to only i % 3
- simplify `number_is_multiple` to only i % 3 == 0
- simplify `number_is_multiple` to only i % 3 == 0 && i % 4 == 0
- switch back to full loop construct
